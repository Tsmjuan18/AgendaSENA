info:
  name: CancelarReserva
  type: http

http:
  method: PATCH
  url: http://localhost:8080/api/reservas/6/cancelar
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
