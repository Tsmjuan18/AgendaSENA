info:
  name: CrearAmbiente
  type: http

http:
  method: POST
  url: http://localhost:8080/api/ambientes
  body:
    type: json
    data: |-
      {
        "nombre": "Sala de Conferencias",
        "tipo": "SALA",
        "capacidad": 40,
        "activo": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​