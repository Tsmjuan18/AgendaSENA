info:
  name: ReservaInactiva
  type: http

http:
  method: POST
  url: http://localhost:8080/api/reservas
  body:
    type: json
    data: |-
      {
        "ambienteId": 5,
        "nombreInstructor": "Instructor X",
        "fechaInicio": "2026-06-24T10:00:00",
        "fechaFin": "2026-06-24T12:00:00",
        "numeroAprendices": 10
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​